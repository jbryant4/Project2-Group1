<div class="comments" id="comment-text2">
    <section class="comment" >
        <div class="text" >
            {{comment_text}}
        </div>
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        
    </section>
</div>